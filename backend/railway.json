{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "healthcheckPath": "/health",
    "preDeployCommand": "python -c \"from pathlib import Path; import sys; sys.path.insert(0, str(Path.cwd())); from main import _run_startup_migrations; _run_startup_migrations()\"",
    "restartPolicyMaxRetries": 10,
    "restartPolicyType": "ON_FAILURE",
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port ${PORT}"
  }
}
